# 2501098-7-

## 2501098 이은지 파이썬 실습 교재 7번쨰 과제...
### 중요한 함수에 대하여 정리하고 과제에 대해 알아보자....

---

## 함수(Funtion)
-> 한 번 작성해 놓으면 재사용가능!
-> 코드용도구분/ 코드 재활용 / 실수 DOWN

---

1. **함수는 어떻게 부르나요?**
+ def 함수이름(): -> 정의하다라는 def 키워드
  
  코드

  -> 중요한 것 _콜론(:)_ 잊으면 안됨

+ 함수 실행시, 이름만 기억하고 넘김 그리고 함수를 부르면
  그때 아 그거 있었지 ~하고 찾아간다

  (중요한 건 파이썬에서 쓰는 함수인지 확인후 사용자정의함수를 찾음)

+ 파이썬은 순서대로 내려가는 애라 함수 호출을 먼저하면 -> **에러**

+ def함수 이름(매개변수):
  
    코드

  함수이름(인수들)

---

2. **함수에 어떤 게 쓰이나요?**
+ 함수안에 _retunr_을 이용하여 값을 함수 밖으로 보낼수 있음!
  
  > 이러지 않으면 함수 안에 있는 것은 **지역함수**라서 밖 사용이 안됨-> 그래서 None 나옴
  
  > **전역함수면 괜찮을수도**

+ 결과를 출력하면..(만약 여러개인 경우 -> _튜플_로 묶어 나옴
  > 왜? -> 선언된 게 값이 변하면 안 되니까 알아서 묶어줌
  
  > 하나씩 가져오려면 인덱스를 이용하자
  
+ 함수는 **스택방식(stack)**으로 쌓음!
  > 배라에서 아이스크림 원하는 맛 퍼주고.. 처음 맛 먹으려면 위 부터 다 처리해야됨
  
  > 그래서 부를 때 추가하고 다 끝나면 지움

---

3. **함수 호출과정**
+ 리턴해서 부를때 안에 함수 사용가능!(어차피 결과값이 보내지는 거라서)

---

>>> 여기까지 알면 문제는 풀 수 있다

#### 하
### 지
## 만
# !

우리는 더 알아보도록 하겠다

---

1. **언패킹?, 패킹?**
   
+ 그것은 인수를 순서대로 넣을 때, 리스트나 튜플 이용 가능
  
#### 패킹
+ 함수(*리스트) -> **한번에** 함수로 전달!!
  
+ 함수(*튜플)   -> **'별' 꼭 있어야됨**
  > 이때 _매개변수 개수 == 요소 개수_

+ 함수 안에 키랑 값 넣는 것 (교재 5인가 해당) 가능하다
  > 함수(키워드=값)

#### 언패킹
+ 딕셔너리로 하는 것 ... *키는 반드시 문자열 !*
  
+ 함수(**딕셔너리)
  > 별이 하나다? -> 키값 / 별이 두 개다? -> value값 전달
  > 매개변수와 딕셔너리의 이름(키) 다르면 **에러!!!!**
  
+ 또한 for a,b in 딕셔너리.items(): -> 다 가져오는 것(키,값)

+ def 함수이름(매개변수=값): -> 기본값 지정 완료!!!
  > 중요한 건 값 지정이 **꼭 맨 뒤**
        코드

---

과제는 기본 내용을 바탕으로 하면 충분히 할 수 있을 줄 알았는데 교재를 엄청 들락날락 했다.
부족한 게 많으니 직접 문제를 손으로 컴터로 해볼 시간이 필요하다고 생각한다.

부족한 부분은 **응용**
